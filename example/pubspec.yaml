name: openmhealth_schemas_example
description: Demonstrates how to use the openmhealth_schemas plugin.

environment:
  sdk: '>=1.20.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  openmhealth_schemas:
    path: ../
